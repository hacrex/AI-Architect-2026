global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'inference-server'
    static_configs:
      - targets: ['inference-server:8003']
    metrics_path: '/metrics'
